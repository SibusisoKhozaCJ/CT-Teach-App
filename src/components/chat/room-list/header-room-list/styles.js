import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    position: 'absolute',
    right: 0,
    color: 'grey',
  },
  svgCloseIcon: {
    fontSize: '2.5rem',
  },
}));

export default useStyles;
